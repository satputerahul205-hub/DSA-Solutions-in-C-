#include<bits/stdc++.h>
using namespace std;
void   buyandsell(int *prices,int n){
    int buymin[100000];
    buymin[0]=INT_MAX;
    for(int i=1;i<n;i++){
        buymin[i]=min(buymin[i-1],prices[i-1]);
    //     cout<<buymin[i]<<" "<<endl;
    // }
    }
  int maxprofit=0;
  int profit[100];
  for(int i=0;i<n;i++){
     profit[i] = prices[i]-buymin[i]; 
     cout<<profit[i];
  }
}
int main(){
    int prices[]={7,1,5,3,6,4};
  int n=sizeof(prices)/4;
  buyandsell(prices,n);

}
